name: terraform-lint

on: [pull_request]

jobs:
  linting:
    runs-on: ubuntu-latest

    env:
      TAG: ${{ github.HEAD_REF }}

    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Lint Terraform
        uses: actionshub/terraform-lint@main
